---
# defaults file for ansible-role-telegraf
apt_key_path: /etc/apt/trusted.gpg.d

# influx APT repository info
telegraf_apt_repo: "deb [arch=amd64 signed-by={{ apt_key_path }}/influxdb2.gpg] https://repos.influxdata.com/debian stable main"
telegraf_apt_key_url: "https://repos.influxdata.com/influxdata-archive_compat.key"
telegraf_apt_package:
  - telegraf
telegraf_apt_service: telegraf
telegraf_urls:
  - "http://graph.ipa.ob.me:8086"
telegraf_organization: "oilboot"
telegraf_bucket: "telegraf"
telegraf_precision: "s"
telegraf_input_systems_net_interfaces:
  - "lo"
  - "eth0"